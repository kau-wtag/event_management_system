<h1><%= @category.new_record? ? 'Create New Category' : 'Edit Category' %></h1>

<%= render 'form', category: @category %>

<%= link_to 'Back to Categories', admin_categories_path, class: 'btn btn-secondary' %>
