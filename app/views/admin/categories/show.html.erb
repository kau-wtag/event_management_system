<h1><%= @category.name %></h1>

<%= link_to t('admin.categories.edit'), edit_admin_category_path(@category), class: 'btn btn-warning' %>
<%= link_to t('admin.categories.back_to_categories'), admin_categories_path, class: 'btn btn-secondary' %>
