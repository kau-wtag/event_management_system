<h1>Forgot Your Password?</h1>
<%= form_with url: password_resets_path, method: :post do %>
  <div class="field">
    <%= label_tag :email %>
    <%= email_field_tag :email, params[:email] %>
  </div>
  <div class="actions">
    <%= submit_tag "Reset Password" %>
  </div>
<% end %>
